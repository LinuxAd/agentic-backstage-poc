nodeLinker: node-modules

yarnPath: .yarn/releases/yarn-4.4.1.cjs
injectEnvironmentFiles:
  # Optional (trailing "?"): present for local `yarn dev`, absent in the Docker
  # build context. Without the "?", yarn errors ENOENT when .env is missing.
  - ".env?"
